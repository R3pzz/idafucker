### Introspect project structure
---
Introspect is the core executable application layer. It essentially provides an OS abstraction
layer for several application-related modules. It is structured as follows:
1. introspect/resources: Provides a solid framework for effectively managing application resources.
1. introspect/runtime: Provides an application runtime layer for different OSes.
